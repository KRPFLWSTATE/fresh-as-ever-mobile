/**
 * @deprecated Pass 26 — import from `@/lib/listingShare` instead.
 * Re-exports preserved for backward compatibility.
 */
export {
  buildShelfDeepLink,
  buildShelfWebLink,
  buildShelfWhatsAppMessage,
  buildWhatsAppShareUrl,
} from '@/lib/listingShare';
