# Copy to `.env.sentry-build-plugin` (gitignored). Used by @sentry/react-native for release uploads.
# Create token: https://sentry.io/settings/account/api/auth-tokens/ (scopes: project:releases, org:read)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=flwstate-media-kx
SENTRY_PROJECT=react-native
