# Work-checker sign-off (per slice)

Copy into PR description or attach log.

1. **Coverage:** traceability rows for this slice → Done / Gap / N/A + file paths.
2. **Regression grep:** no stray `console.log`; no new hardcoded `#fff` outside QR tokens; a11y labels on new pressables.
3. **Gate A:** `npm run ci` exit code ______.
4. **MCP log:** Xcode / Appium / Mobile-Next — used or N/A ______.
5. **Recommendation:** merge | merge with follow-ups | block ______.
