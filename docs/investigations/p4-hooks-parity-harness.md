# Hook parity harness stub (**§ `p4-hook-discoverbags-rpc-vs-fallback-same-count` + impact cache)**

| Harness | Planned |
|---------|---------|
| Discover counts vs web | Script comparing `nearby_bags` row counts staging |
| Checkout cash rule parity | Extend `CheckoutScreen` with extractable reducer |
| Order status enums | Snapshot JSON from PostgREST |
| Impact cache invalidate | Tie to role change + locality hash |
