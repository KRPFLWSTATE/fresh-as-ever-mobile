# Android Gradle / Kotlin matrix (**§ `p1-android-gradle-kotlin-agp-matrixdoc`**)

Built with RN **0.85** Gradle template. Maintain:

- JDK **17** toolchain (Temurium / Android Studio bundled).
- `android/settings.gradle.kts`, `gradle-wrapper.properties`: track AGP bumps with RN release notes.
- After AGP bumps run `./gradlew :app:assembleDebug` smoke.

Pod install checklist (paired): README **CocoaPods paths** (`§p1-podfile-pod-lock-ci-deployment`).
