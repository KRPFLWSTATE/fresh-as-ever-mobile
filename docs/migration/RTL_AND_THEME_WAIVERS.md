# RTL / token prep (**§ `p3-token-theme-rtl-directionality`**)

Prefer `marginStart` / `marginEnd` going forward when touching layouts. RN tree not yet audited for full RTL flipping — waive until product prioritizes mirrored layouts; track regressions alongside dynamic type backlog in `PROFILE` screens.
