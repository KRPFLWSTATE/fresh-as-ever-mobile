# Singular `/bag/:id` alias (**§ `p0-doc-singular-plural-route-bag-vs-bags-alias-table`**)

Source of truth: `MIDDLEWARE_MATRIX.md` Regex row `/bag/:id`.

RN implementation mirrors Next redirect inside `normalizeIncomingLinkPath()` → canonical `bags/:id` tree.
