# Wrong device clock + PayHere (**§ `p5-ant-payhere-clock-skew-jwt-visible-message-ant`**)

If hashed tokens reject:

1. Show copy: **“Adjust device clock to automatic timezone, then retry.”**
2. Log correlation id (`order_id` hash request).
3. Support runbook appendix in hotfix playbook.
