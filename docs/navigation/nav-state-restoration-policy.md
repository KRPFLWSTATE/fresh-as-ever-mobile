# Navigation persistence scope (**§ `p2-ant-state-restoration-asyncstorage-opt-in-scope-doc`**)

Checkout + PayHere modals deliberately **avoid** persisted nav state snapshots (financial flows). Tabs + benign leaf screens may adopt AsyncStorage **opt-in** when product approves UX contract.
