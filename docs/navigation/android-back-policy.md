# Android hardware back (**§ `p2-ant-back-handler-android-root-exit-twice-ant`**)

Defaults: RN stack consumes back until root; Merchant / Customer stacks exit to previous tab shell. Document product-specific overrides when implementing double-tap-exit prompts.
